language: php

php:
  - 5.6
  - 7.0
  - 7.1
  - 7.2
  - 7.3
  - 7.4

before_script:
  - composer self-update
  - composer install --prefer-source --no-interaction --dev

script:
  - php vendor/bin/phpcs --standard=PSR2 docs/examples src tests
  - php vendor/bin/phpunit tests